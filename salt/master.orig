#HOTROD SALT

root_dir: ./salt/
pki_dir: pki/master
cachedir: run/master
sock_dir: ///tmp

pillar_roots:
  base:
    - ./pillar  
    
file_roots:
  base:
    - ./project/hotrod
    - ./local  
    - ./srv
    - ./

user: {{ salt_user }}

