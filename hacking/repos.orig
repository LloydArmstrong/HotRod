declare -a repos=(
{% for prefix in repos -%}
{% for repo in repos[prefix] -%}
    git://{{ prefix }}/{{ repo }}
{% endfor %}
{%- endfor %})

declare -a projects=(
{% for prefix in repos -%}
{% for repo in repos[prefix] -%}
    {{ repo }}
{% endfor %}
{%- endfor %})

